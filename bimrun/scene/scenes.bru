meta {
  name: scenes
  type: http
  seq: 1
}

get {
  url: {{base_url}}/apis/scene-portlet/api/scenes?projectId={{projectId}}&groupId={{groupId}}&max={{max}}
  body: none
  auth: none
}

params:query {
  projectId: {{projectId}}
  groupId: {{groupId}}
  max: {{max}}
}

vars:pre-request {
  projectId: 77974
  groupId: 27702
  max: 100
}
