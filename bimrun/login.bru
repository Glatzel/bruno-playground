meta {
  name: login
  type: http
  seq: 1
}

post {
  url: {{base_url}}/apis/user-portlet/api/login
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {"username": "{{username}}", "password": "{{password}}"}
}
