meta {
  name: projects
  type: http
  seq: 1
}

get {
  url: {{base_url}}/apis/project-portlet/api/projects?max={{max}}&name
  body: none
  auth: none
}

params:query {
  max: {{max}}
  name:
}

vars:pre-request {
  max: 5
  name:
}
